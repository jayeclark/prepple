package com.prepple.api.model.shared;

/**
 * Enum of the types of currencies in which mentors can charge for sessions
 * Will be only USD in practice for the foreseeable future but is being built in
 * now to allow for future expansion
 */
public enum Currency {
    USD,
    CAD
}
